server {
    listen       80;
    listen  [::]:80;
    server_name  localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Handle specific static routes
    location = /about {
        try_files /about.html =404;
    }

    location = /legal/privacy {
        try_files /legal/privacy.html =404;
    }

    location = /legal/terms {
        try_files /legal/terms.html =404;
    }

    # Default route handling (SPA support)
    location / {
        try_files $uri $uri/ /index.html;
    }

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    # Example PHP config (commented out)
    #location ~ \.php$ {
    #    proxy_pass   http://127.0.0.1;
    #}

    #location ~ \.php$ {
    #    root           html;
    #    fastcgi_pass   127.0.0.1:9000;
    #    fastcgi_index  index.php;
    #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
    #    include        fastcgi_params;
    #}

    # Block .htaccess access
    #location ~ /\.ht {
    #    deny  all;
    #}
}
